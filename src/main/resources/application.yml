spring:
  thymeleaf:
    cache: false
    suffix: .html
    mode: HTML
    encoding: utf-8
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
    #数据库连接配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.16.5.24:3306/sxgd?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: sxgd
    password: sxgd

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.xiaoi.exp.voice.entity
  #开启驼峰命名
  configuration:
    map-underscore-to-camel-case: true